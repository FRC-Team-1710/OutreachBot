package org.usfirst.frc.team1710.robot;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class AutoChallengeGroup extends CommandGroup {

    public AutoChallengeGroup() {
    	//there are 1000 milliseconds in one second
    	//addSequential(new ForwardTime(power (decimal, 0 to 1), time (milliseconds)));
    	//addSequential(new Turn4Time(power (decimal 0 to 1), time (milliseconds)));
    	//example: addSequential(new ForwardTime(0.5, 2000));
    	
    }
}
